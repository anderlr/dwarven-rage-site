import { Header, Title } from './styles';
import { Grid } from './../globalStyles';
export default function Navbar() {
  return (
    <Grid>
      <Header>
        <Title>Title</Title>
      </Header>
    </Grid>
  );
}
